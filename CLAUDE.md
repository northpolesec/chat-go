# chat-go

Go port of the Vercel Chat SDK.

`PORTING.md` is the ledger of pins, dropped scope, and file rows.

To bump `vercel/chat` or remend, or to port one named adapter, follow `.claude/skills/porting/SKILL.md`.
